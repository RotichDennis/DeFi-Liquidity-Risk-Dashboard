# @title 1. Install Dependencies
# Install system libraries for some causal packages
!apt-get install -y libgmp-dev libmpfr-dev libmpc-dev

# Install Python packages
!pip install -q requests gql pandas duckdb pyarrow aiohttp sqlalchemy streamlit plotly clickhouse-connect tqdm python-dotenv
!pip install -q lightgbm torch torchvision torchaudio scikit-learn statsmodels dowhy pyinform pyngrok darts

print("✅ Dependencies installed.")

